Example for CommandLineRunner and ApplicationRunner.
Hit "http://localhost:8080/response" to see Controller method response which will be returned as response.
Check console to see that CommandLineRunner/ApplicationRunner text got printed before calling any API.
